---
import { Image } from "astro:assets";
import logo from "../../assets/paw-drawing-white.svg";
import phone from "../../assets/phone.svg";
import mail from "../../assets/mail.svg";
import atSign from "../../assets/at-sign.svg";
import awsLogo from "../../assets/aws-color.svg";
import resume from "../../assets/resume.svg";
import { db } from "../../lib/database";
---

<div class="container">
  <div class="up-container">
    <div class="credentials">
      <h1>{`${db.name.toUpperCase()} ${db.surname.toUpperCase()}`}</h1>
      <h3>{db.profession.toUpperCase()}</h3>
    </div>
    <div>
      <Image src={logo} alt="logo" width={70} height={70} />
    </div>
  </div>

  <div class="down-container">
    <div class="contacts-container">
      <div class="contacts">
        <Image src={phone} alt="phone" width={20} height={20} />
        <a href={`tel:${db.phone}`}>{db.phone}</a>
      </div>
      <div class="contacts">
        <Image src={mail} alt="mail" width={20} height={20} />
        <a href={`mailto:${db.email}`}>{db.email}</a>
      </div>
      <div class="contacts">
        <Image src={atSign} alt="at-sign" width={20} height={20} />
        <a href={`https://${db.website}`} target="_blank">{db.website}</a>
      </div>
    </div>
    <div class="footer-container">
      <a href={db.resume} target="_blank">
        <Image src={resume} alt="resume" width={100} height={50} />
      </a>
      <div class="aws">
        <Image src={awsLogo} alt="aws-logo" width={50} height={50} />
        <p>Certified</p>
      </div>
    </div>
  </div>
</div>

<style>
  h1 {
    margin: 0;
  }

  h3 {
    margin: 0;
    font-weight: 400;
  }

  p {
    margin: 0;
  }

  a {
    color: white;
    text-decoration: none;
  }

  a:hover {
    text-decoration: underline;
  }

  .container {
    margin: 1rem;
    display: flex;
    flex: 1;
    flex-direction: column;
    justify-content: space-between;
  }

  .up-container {
    display: flex;
    flex: 1;
    justify-content: space-between;
  }

  .credentials {
    padding-top: 4rem;
    padding-left: 1rem;
  }

  .down-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.5rem;
  }

  .contacts-container {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .contacts {
    display: flex;
    gap: 0.5rem;
  }

  .footer-container {
    display: flex;
    align-items: center;
    gap: 2rem;
  }

  .aws {
    display: flex;
    flex-direction: column;
    align-items: center;
    font-size: small;
  }
</style>
